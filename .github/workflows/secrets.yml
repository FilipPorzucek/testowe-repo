name: Sekrety

on:
  push:
  workflow_dispatch:

jobs:
  use-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Uzyj sekretu
        env:
          TOKEN: ${{ secrets.API_TOKEN }}
        run: echo "Dlugosc tokenu ${#TOKEN}"